# Datos

Esta carpeta conserva solo notebooks exploratorios. Los datos brutos originales no se incluyen en el repositorio publico.

Los notebooks de `SB013/` documentan las primeras pruebas de lectura, segmentacion, tiles, ROIs y depuracion del caso SB013. Son parte del historial metodologico, pero el pipeline final se encuentra en `Registration/DeepLearning/`.

